# TeamTrustGate 🛡️

> ИИ-агент для автоматической обработки клиентских запросов от сейлзов в продуктовую команду.  
> Telegram → LLM → Jira. Без бардака, без дублей, без ручной работы.

**Стек:** Python 3.11 · Telegram Bot API · Gemini 2.5 Flash · DeepSeek (резерв) · Jira REST API v2 · SQLite · Railway

---

## Зачем это нужно

Сейлзы каждый день получают запросы от клиентов — «хотим фичу X», «не работает Y», «конкуренты уже умеют Z». Раньше это шло в общий чат или в Jira вручную: без структуры, без приоритетов, с дублями и вечным бардаком в бэклоге.

**TeamTrustGate решает это автоматически:**

- Сейлз пишет запрос в Telegram **своими словами** — никаких форм, никакого Jira
- ИИ извлекает суть, оценивает бизнес-риск и приоритет по методологии RICE
- Бот создаёт структурированный тикет в Jira
- Продуктовая команда получает готовый, оценённый запрос
- Сейлз получает уведомление когда статус его тикета изменится

---

## Архитектура

```
Сейлз (Telegram)
       │
       ▼
  [ bot.py ]  ←── Telegram Bot API
       │
       ├──► [ llm_adapter.py ]
       │         ├── Gemini 2.5 Flash (основной)
       │         └── DeepSeek Chat (резерв при отказе)
       │
       ├──► [ deduplicator.py ]  ← батч-дедупликация
       │
       ├──► [ scorer.py ]  ← Weighted RICE скоринг
       │
       ├──► [ jira_client.py ]  ← Jira REST API v2
       │
       └──► [ state_manager.py ]  ← SQLite (сессии + тикеты + статистика)
```

---

## Флоу обработки запроса

```
1. Сейлз пишет запрос в Telegram
          │
          ▼
2. LLM анализирует сообщение (extraction.md)
   └── confidence < 0.8? → уточняющий вопрос (до 3 раундов)
          │
          ▼
3. Дедупликация — сравнивает с бэклогом Jira
   └── дубликат найден? → добавляет комментарий, уведомляет сейлза
          │
          ▼
4. RICE-скоринг (scoring.md)
   └── Reach × Impact × Confidence × Strategy Fit → 0–400 баллов
          │
          ▼
5. Превью тикета для подтверждения
   └── [✅ Создать] [✏️ Уточнить] [❌ Отменить]
          │
          ▼
6. Тикет создаётся в Jira
          │
          ▼
7. Polling каждые 5 минут → уведомление при смене статуса
```

---

## Функциональность

### Обработка запросов
- Приём запросов через Telegram
- Семантический анализ: очистка от эмоций, паники и сленга продажников
- Уточняющие вопросы при недостатке данных (до 3 раундов)
- Фильтрация спама и нерелевантных запросов (`should_reject`)

### Дедупликация
- Батч-сравнение нового запроса со всеми тикетами за N дней за **один** LLM-вызов
- Семантическое сравнение через LLM (не просто ключевые слова)
- Keyword fallback (Jaccard similarity) при недоступности LLM
- При дубликате — добавление комментария в существующий тикет

### Скоринг (Weighted RICE)
- **Reach** — охват: один клиент / сегмент / все клиенты (1 / 5 / 10)
- **Impact** — риск потери выручки (1–10)
- **Confidence** — уверенность в данных (0.0–1.0)
- **Strategy Fit** — соответствие продуктовой стратегии (1–10)
- **Enterprise-множитель:** если `is_enterprise_risk=true` и `revenue_at_risk≥9` → принудительный подъём reach до 5 и strategy_fit до 10
- Итоговый скор 0–400 → Highest / High / Medium / Low

### Telegram UX
- Превью тикета перед созданием — сейлз видит что распознал ИИ и подтверждает
- Inline-кнопки под каждым тикетом (статус, удалить, сменить статус)
- Уведомления при изменении статуса тикета (polling каждые 5 минут)
- Параллельная обработка запросов (`concurrent_updates=True`)

### Ролевая модель
- **Сейлз** (`ALLOWED_USERNAMES`) — создаёт тикеты, управляет **только своими**
- **Админ** (`ADMIN_USERNAMES`) — полный доступ + `/stats` + `/export`
- Ownership проверяется по нормализованному username при каждом действии
- Двойная проверка прав: на кнопке и при подтверждении (защита от устаревших кнопок)

### Jira интеграция
- Создание структурированных Story-тикетов с приоритетом и лейблами
- Смена статуса через Telegram (▶️ В работу / ✅ Готово / 🔄 На проверку / 🚫 Отклонить)
- Удаление тикета с двойным подтверждением
- Просмотр последних 5 тикетов (`/list`)
- Circuit breaker: 5 ошибок подряд → блокировка на 5 минут

### Аналитика и экспорт
- `/stats` — статистика за 30 дней: тикеты по приоритетам, статусам, топ сейлзов
- `/export [дней]` — выгрузка всех тикетов в CSV с BOM для Excel/Google Sheets

### Отказоустойчивость
- Автоматический failover: Gemini → DeepSeek при недоступности
- Retry с экспоненциальным backoff (3 попытки, временные ошибки)
- RAW-тикет при полном отказе ИИ — запрос не теряется
- Сохранение упавших запросов в SQLite для анализа

---

## Структура проекта

```
teamtrustgate/
├── bot.py              # Telegram bot, хендлеры, polling, роли
├── config.py           # Конфигурация из env переменных
├── llm_adapter.py      # Провайдеры LLM + failover оркестратор
├── jira_client.py      # Jira REST API + circuit breaker
├── deduplicator.py     # Батч-дедупликация + keyword fallback
├── scorer.py           # Weighted RICE скоринг
├── state_manager.py    # SQLite: сессии, tracking, статистика
├── parse_utils.py      # Безопасный парсинг числовых полей LLM
├── prompts/
│   ├── extraction.md   # Анализ сообщения сейлза
│   ├── dedup.md        # Дедупликация (одиночная)
│   ├── dedup_batch.md  # Батч-дедупликация
│   └── scoring.md      # RICE скоринг (роль CPO)
├── requirements.txt
├── runtime.txt         # python-3.11.9
├── Procfile
└── .env.example
```

---

## Промпты

Все промпты в папке `prompts/` в формате `.md`. Поддерживаются также `.txt` — бот ищет оба формата автоматически.

| Файл | Роль | Назначение |
|------|------|-----------|
| `extraction.md` | Senior Product Analyst | Анализ сырого сообщения, извлечение структурированных бизнес-данных |
| `dedup.md` | Система дедупликации | Сравнение одного запроса с одним тикетом |
| `dedup_batch.md` | Система дедупликации | Один запрос против всего бэклога за один вызов |
| `scoring.md` | Chief Product Officer | Weighted RICE скоринг, расстановка приоритетов |

---

## Переменные окружения

### Обязательные

| Переменная | Описание |
|-----------|---------|
| `TELEGRAM_TOKEN` | Токен Telegram бота (от @BotFather) |
| `LLM_API_KEY` | API ключ основного LLM провайдера |
| `JIRA_URL` | URL Jira instance (`https://company.atlassian.net`) |
| `JIRA_EMAIL` | Email аккаунта Jira |
| `JIRA_API_TOKEN` | API токен Jira (Settings → Security → API tokens) |
| `JIRA_PROJECT_KEY` | Ключ проекта в Jira (например `TT`) |

### Опциональные

| Переменная | По умолчанию | Описание |
|-----------|:---:|---------|
| `LLM_PROVIDER` | `gemini` | Провайдер: `gemini` / `deepseek` / `openai` / `anthropic` |
| `DEEPSEEK_API_KEY` | — | API ключ DeepSeek (резервный провайдер) |
| `GEMINI_MODEL` | `gemini-2.5-flash` | Название модели Gemini |
| `DEEPSEEK_MODEL` | `deepseek-chat` | Название модели DeepSeek |
| `ALLOWED_USERNAMES` | — | Telegram username сейлзов через запятую (пусто = все) |
| `ADMIN_USERNAMES` | — | Telegram username админов через запятую |
| `PRODUCT_STRATEGY` | — | Описание продуктовой стратегии для скоринга |
| `CONFIDENCE_THRESHOLD` | `0.7` | Порог уверенности для уточнений |
| `DEDUP_DAYS` | `90` | Окно дедупликации в днях |
| `MAX_CLARIFICATION_ROUNDS` | `3` | Максимум раундов уточнений |
| `JIRA_TIMEOUT` | `10` | Таймаут запросов к Jira (секунды) |
| `LLM_TIMEOUT` | `30` | Таймаут запросов к LLM (секунды) |
| `LOG_LEVEL` | `INFO` | Уровень логирования |
| `DB_PATH` | `teamtrustgate.db` | Путь к SQLite базе данных |

---

## Деплой на Railway

### 1. Подготовка

```bash
git clone https://github.com/karabekdamir428/teamtrustgate0
cd teamtrustgate0
```

### 2. Создание проекта

1. Зайди на [railway.app](https://railway.app) и создай новый проект
2. Подключи GitHub репозиторий
3. Railway автоматически определит Python-проект

### 3. Переменные окружения

В Railway → твой сервис → **Variables** добавь:

```
TELEGRAM_TOKEN=your_token_here
LLM_PROVIDER=gemini
LLM_API_KEY=your_gemini_key
DEEPSEEK_API_KEY=your_deepseek_key
JIRA_URL=https://yourcompany.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=your_jira_token
JIRA_PROJECT_KEY=TT
ALLOWED_USERNAMES=ivanov,petrov,sidorova
ADMIN_USERNAMES=your_telegram_username
PRODUCT_STRATEGY=Описание вашей продуктовой стратегии
GEMINI_MODEL=gemini-2.5-flash
DEEPSEEK_MODEL=deepseek-chat
```

### 4. Деплой

Просто сделай push в `main` — Railway задеплоит автоматически.

**Procfile:**
```
web: python bot.py
```

**runtime.txt:**
```
python-3.11.9
```

---

## Локальная разработка

```bash
# Клонируем
git clone https://github.com/karabekdamir428/teamtrustgate0
cd teamtrustgate0

# Устанавливаем зависимости
pip install -r requirements.txt

# Копируем и заполняем .env
cp .env.example .env
# Заполни .env своими ключами

# Запускаем
python bot.py
```

---

## Команды бота

### Для всех пользователей

| Команда | Описание |
|---------|---------|
| `/start` | Приветствие, роль и список команд |
| `/cancel` | Отменить текущую сессию |
| `/status TT-42` | Статус тикета с кнопками управления |
| `/list` | Последние 5 твоих тикетов |
| `/delete TT-42` | Удалить свой тикет (с подтверждением) |

### Только для администраторов 👑

| Команда | Описание |
|---------|---------|
| `/stats` | Аналитика за 30 дней: приоритеты, статусы, топ сейлзов |
| `/export [дней]` | Выгрузка всех тикетов в CSV (по умолчанию 90 дней) |

### Inline-кнопки под тикетом

| Кнопка | Кто может | Действие |
|--------|:---:|---------|
| 🔗 Открыть в Jira | Все | Прямая ссылка на тикет |
| 📊 Статус | Все | Текущий статус |
| 🗑 Удалить | Владелец / Админ | Удаление с подтверждением |
| ▶️ В работу | Владелец / Админ | Смена статуса |
| ✅ Готово | Владелец / Админ | Закрыть тикет |
| 🔄 На проверку | Владелец / Админ | Отправить на ревью |
| 🚫 Отклонить | Владелец / Админ | Отклонить тикет |

---

## RICE-скоринг

### Формула

```
total_score = Reach × Impact × Confidence × Strategy_Fit
```

Максимум: **400 баллов**

### Компоненты

| Компонент | Значения | Источник |
|-----------|---------|---------|
| Reach | 1 (один клиент) / 5 (сегмент) / 10 (все) | LLM из extraction |
| Impact | 1–10 (риск потери выручки) | LLM из extraction |
| Confidence | 0.0–1.0 (уверенность в данных) | LLM из extraction |
| Strategy Fit | 1–10 (соответствие стратегии) | LLM из scoring |

### Таблица приоритетов

| Балл | Приоритет | Действие |
|------|-----------|---------|
| ≥ 250 | 🔴 Highest | Немедленно в спринт |
| 120–249 | 🟠 High | В ближайший квартал |
| 50–119 | 🟡 Medium | В бэклог |
| < 50 | 🟢 Low | Отложить |

### Enterprise-множитель

Если `is_enterprise_risk = true` **И** `revenue_at_risk ≥ 9`:
- `reach_score` принудительно поднимается до минимум **5**
- `strategy_fit_score` принудительно устанавливается в **10**

---

## Ролевая модель

```
┌─────────────────────────────────────────┐
│              ADMIN_USERNAMES            │
│  ✅ Создание тикетов                    │
│  ✅ Управление своими тикетами          │
│  ✅ Управление чужими тикетами          │
│  ✅ /stats — аналитика                  │
│  ✅ /export — выгрузка CSV              │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│             ALLOWED_USERNAMES           │
│  ✅ Создание тикетов                    │
│  ✅ Управление своими тикетами          │
│  ❌ Управление чужими тикетами          │
│  ❌ /stats                              │
│  ❌ /export                             │
└─────────────────────────────────────────┘
```

**Настройка в Railway Variables:**
```
ALLOWED_USERNAMES=ivanov,petrov,sidorova
ADMIN_USERNAMES=karabekdamir,product_manager
```

Username без `@`, через запятую, регистр не важен.

---

## Как добавить нового пользователя

1. Зайди в Railway → Variables
2. Найди `ALLOWED_USERNAMES`
3. Добавь Telegram username через запятую
4. Railway передеплоит автоматически — готово

Никаких изменений в коде не нужно.

---

## Зависимости

```
python-telegram-bot[job-queue]==21.3
requests==2.32.3
google-generativeai==0.8.3
python-dotenv==1.0.0
aiohttp==3.9.0
pydantic==2.10.0
```

`[job-queue]` — обязательный суффикс для фоновых уведомлений об изменении статуса.

---

## Лицензия

MIT
